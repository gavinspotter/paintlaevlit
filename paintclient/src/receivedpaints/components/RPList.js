import React from "react"

const RPList = (props) => {
    if (props.items.length === 0) {
        return (
            <div>
                <h2>no paints found</h2>
            </div>
        )
    }
}

export default RPList